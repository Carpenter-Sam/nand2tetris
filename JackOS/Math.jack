// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/12/Math.jack
/**
 *  A library of commonly used mathematical functions.
 *  All functions runs in O(n), where n is the number of bits used
 *  for representing a two's complement integer value (16 in the Hack computer). 
 *  Note: Jack compilers implement multiplication and division
 *  using calls to OS functions in this class.
 */
class Math {
    static int n;             // Number of bits used for representing a two's complement integer
    static Array powersOfTwo; // Stores 2^0, 2^1, 2^2,..., 2^(n-1)

    // Initializes the Math library.
    function void init() {
        let n = 16;
        let powersOfTwo = Array.new(16);
        let powersOfTwo[0] = 1;
        let powersOfTwo[1] = 2;
        let powersOfTwo[2] = 4;
        let powersOfTwo[3] = 8;
        let powersOfTwo[4] = 16;
        let powersOfTwo[5] = 32;
        let powersOfTwo[6] = 64;
        let powersOfTwo[7] = 128;
        let powersOfTwo[8] = 256;
        let powersOfTwo[9] = 512;
        let powersOfTwo[10] = 1024;
        let powersOfTwo[11] = 2048;
        let powersOfTwo[12] = 4096;
        let powersOfTwo[13] = 8192;
        let powersOfTwo[14] = 16384;
        let powersOfTwo[15] = -32767;
        let powersOfTwo[15] = powersOfTwo[15] - 1;
        return;
    }

    /** Returns the product of x and y. 
     *  When a Jack compiler detects the multiplication operator '*'
     *  in an expression, it handles it by invoking this method. 
     *  Thus, in Jack, x * y and Math.multiply(x,y) return the same value. */
    function int multiply(int x, int y) {
        var int sum, shiftedX, iter;
        let sum = 0;
        let shiftedX = x;
        let iter = 0;

        // Loops through for each bit of y, and moves shiftedX to the left.
        // Then multiplies shiftedX by the corresponding bit of i and add it to the sum.
        // If the corresponding y-bit = 0 then if will zero out the shiftedX temporarily.
        while(iter < n) {
            if (Math.bit(y, iter) = 1) {
                let sum = sum + shiftedX;
            }
            // Same as shiftedX * 2, which is the same as bit-shift left.
            let shiftedX = shiftedX + shiftedX;

            let iter = iter + 1; // iterate iter
        }
        return sum;
    }

    // Returns true if the i-th bit of x is 1, false otherwise.
    function boolean bit(int x, int i) {
        if ((x & powersOfTwo[i]) = 0) {
            return 0;
        } else {
            return 1;
        }
    }

    // Gets 2^x
    function int getTwoPower(int x) {
        return powersOfTwo[x];
    }

    // Finds the modulo of a % mod. Probably super unoptimised.
    function int modulo(int a, int mod) {
        var int calculation;
        let calculation = Math.abs(a);

        while (calculation > (mod - 1)) {
            let calculation = calculation - mod;
        }

        return calculation;
    }

    /** Returns the integer part of x / y.
     *  When a Jack compiler detects the division operator '/'
     *  an an expression, it handles it by invoking this method.
     *  Thus, x/y and Math.divide(x,y) return the same value. */
    function int divide(int x, int y) {
        var int q, posX, posY;
        var boolean negative;

        if (y = 0) {
            do Sys.error(3); // division by zero
        }

        let negative = false;
        if (x < 0) {
            let negative = ~negative;
        }
        if (y < 0) {
            let negative = ~negative;
        }
        let posX = Math.abs(x);
        let posY = Math.abs(y);

        if (posX = posY) {
            if (negative) {
                return -1;
            } else {
                return 1;
            }
        }

        if (posY > posX) {return 0;}

        let q = Math.divideRecursive(posX, 2 * posY);

        if ((posX - (2 * q * posY)) < posY) {
            let q = 2 * q;
        } else {
            let q = 2 * q + 1;
        }

        if (negative) {
            return -q;
        } else {
            return q;
        }
    }

    function int divideRecursive(int x, int y) {
        var int q;

        if ((y > x) | (y < 0)) {return 0;}
        if (x = y) {return 1;}

        let q = Math.divideRecursive(x, 2 * y);
        if ((x - (2 * q * y)) < y) {
            return 2 * q;
        } else {
            return 2 * q + 1;
        }
    }

    /** Returns the integer part of the square root of x. */
    function int sqrt(int x) {
        var int y, j, calc;

        if (x < 0) {
            do Sys.error(4); // Cannot compute square root of a negative number 
        }

        let y = 0;
        let j = (n/2) - 1;
        while (j > -1) {
            let calc = y + powersOfTwo[j]; // y + 2^i
            let calc = calc * calc; // (y + 2^i)^2
            if ( (calc - 1 < x) & (calc > 0) ) {

                let y = y + powersOfTwo[j];
            }
            let j = j - 1;
        }
        return y;
    }

    /** Returns the greater value. */
    function int max(int a, int b) {
        if (a > b) {
            return a;
        } else {
            return b;
        }
    }

    /** Returns the smaller value. */
    function int min(int a, int b) {
        if (a < b) {
            return a;
        } else {
            return b;
        }
    }

    /** Returns the absolute value of x. */
    function int abs(int x) {
        if (x < 0) { // Inverts x if it's less than 0.
            return -x;
        }
        return x;
    }
}
